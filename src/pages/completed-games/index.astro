---
import BaseLayout from '../../layouts/BaseLayout.astro'
import ReviewCard from '../../components/ReviewCard.astro'

import { getCollection } from 'astro:content'
const gamePosts = await getCollection('completed-games')
---

<BaseLayout title="My Completed Games">
  <div class="container mx-auto px-6 md:px-28 mt-12 grid justify-items-center">
    <div class="max-w-2xl mb-12">
      <h1 class="font-bold text-2xl text-center mb-1.5">My Completed Games</h1>
      <p class="text-center">
        Ini adalah project menamatkan beberapa game RPG/JRPG/Story based yang
        akan saya lakukan di tahun 2024-2025.
      </p>
    </div>

    <div class="grid md:grid-cols-2 gap-6 md:gap-4">
      {gamePosts.map((post) => <ReviewCard post={post} />)}
    </div>
  </div>
</BaseLayout>
